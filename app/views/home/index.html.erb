<h1>HOME</h1>

<% if @posts.any? %>
  <% @posts.each do |post| %>
    <%= link_to image_tag(post.asset.url(:small)), user_post_path(current_user, post) %>
  <% end %>
<% else %>
  <p>There are no posts yet!</p>
<% end %>
<%= link_to "New Post", new_user_post_path(current_user) %>
