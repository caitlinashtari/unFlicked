<h1>Post Index</h1>

<% if @posts.any? %>
  <% @posts.each do |post| %>
    <%= link_to image_tag(post.asset.url(:small)), user_post_path(post.owner_id, post) %>
  <% end %>
<% else %>
  <p>There are no posts yet!</p>
<% end %>
